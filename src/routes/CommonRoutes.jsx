import { lazy } from 'react'

// const Reports = lazy(() => import('@/components/common/reports/Reports'));


/**
 * @type {import('react-router-dom').RouteObject[]}
 */
const CommonRoutes = [
    // { path: "reports", element: <Reports /> }
]

export default CommonRoutes;
